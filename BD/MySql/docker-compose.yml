version: '1'

services:
  mysql:
    image: mysql:latest
    ports:
      - "3311:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 12345
      MYSQL_DATABASE: work_market_db
      MYSQL_USER: work_market_db
      MYSQL_PASSWORD: 98765

      volumes:
        mysql_data:/var/lib/mysql

volumes:
  mysql_data:

