# work_market
This utilite helps understand what skills needed by employers

### Building ###

sudo apt-get install python
sudo apt-get install -y docker
pip install docker

docker build -t work_market .
